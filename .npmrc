# Public npm registry — this repo is an out-of-repo Game Hub game (Track D) and MUST resolve @game-hub/*
# from the registry, never from a workspace link back into the platform monorepo. Stated explicitly so a
# stray local .npmrc or a link: override can't quietly reintroduce the coupling this shape exists to avoid.
registry=https://registry.npmjs.org/
